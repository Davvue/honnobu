name: Build and Push Edge

on:
  push:
    branches:
      - development

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    uses: ./.github/workflows/docker-build-push.yml
    with:
      tags: |
        type=raw,value=edge
    secrets: inherit