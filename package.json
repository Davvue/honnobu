{
  "name": "honnobu",
  "version": "0.0.0",
  "private": true,
  "description": "Open-source and self-hosted book tracker",
  "scripts": {
    "init:packages": "pnpm --filter \"@honnobu/*\" build",
    "dev:api": "pnpm run init:packages && pnpm --filter api start:dev",
    "dev:web": "pnpm run init:packages && pnpm --filter web start",
    "build:api": "pnpm run init:packages && pnpm --filter api build",
    "build:web": "pnpm run init:packages && pnpm --filter web build",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "11.8.0",
      "onFail": "download"
    }
  },
  "devDependencies": {
    "prettier": "^3.4.2"
  },
  "type": "module",
  "packageManager": "pnpm@11.8.0"
}
